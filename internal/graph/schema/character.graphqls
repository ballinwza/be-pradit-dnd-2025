type Character {
  id: String!
  user: User!
  name: String!
  avatarImage: String!
  speed: Int!
  initiativePoint: Int!
  hitDice: Int!
}

extend type Query {
  characterById(id: String!): Character!
}
